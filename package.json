{
  "name": "task-tracker",
  "version": "0.1.0",
  "description": "Track Claude Code plans across projects",
  "type": "module",
  "bin": {
    "tracker": "src/cli.ts"
  },
  "scripts": {
    "cli": "bun run src/cli.ts",
    "link": "bun link && ln -sfn $(pwd)/skills/tracker-plan ~/.claude/skills/tracker-plan"
  },
  "devDependencies": {
    "bun-types": "^1.3.8"
  }
}
