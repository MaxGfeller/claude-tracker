{
  "name": "task-tracker",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "cli": "bun run packages/cli/src/cli.ts",
    "link": "cd packages/cli && bun link && ln -sfn $(pwd)/../skills/skills/tracker-plan ~/.claude/skills/tracker-plan"
  }
}
